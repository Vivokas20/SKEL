schema df_products(product_id:int, product_type_code:str, product_name:str, product_price:str);

table df_products(df_products);

query q1
`select product_name,
       product_type_code
from df_products df_products`;

query q2
`select input.product_name,input.product_type_code
 from
  input`;

verify q1 q2;