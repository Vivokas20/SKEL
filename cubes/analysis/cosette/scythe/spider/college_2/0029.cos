schema df_department(dept_name:str, building:str, budget:str);

table df_department(df_department);

query q1
`select dept_name,
       building
from df_department df_department`;

query q2
`select input.dept_name,input.building
 from
  input`;

verify q1 q2;