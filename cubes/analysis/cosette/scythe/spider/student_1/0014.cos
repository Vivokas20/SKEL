schema df_teachers(lastname:str, firstname:str, classroom:int);

table df_teachers(df_teachers);

query q1
`select distinct firstname,
                lastname
from df_teachers df_teachers`;

query q2
`select input.firstname,input.lastname
 from
  input`;

verify q1 q2;