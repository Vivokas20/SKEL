schema df_customers(customer_id:int, customer_details:str);

table df_customers(df_customers);

query q1
`select customer_details
from df_customers df_customers`;

query q2
`select input.customer_details
 from
  input`;

verify q1 q2;