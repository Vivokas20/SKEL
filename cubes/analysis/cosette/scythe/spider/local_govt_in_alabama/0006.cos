schema df_services(service_id:int, service_type_code:str);

table df_services(df_services);

query q1
`select service_type_code
from df_services df_services`;

query q2
`select input.service_type_code
 from
  input`;

verify q1 q2;