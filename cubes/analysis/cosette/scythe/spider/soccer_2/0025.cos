schema df_college(cname:str, state:str, enr:int);

table df_college(df_college);

query q1
`select cname,
       state,
       min(enr)
from df_college df_college
group by state`;

query q2
`input`;

verify q1 q2;