schema df_media_types(id:int, name:str);

table df_media_types(df_media_types);

query q1
`select name
from df_media_types df_media_types`;

query q2
`select distinct name
from input0`;

verify q1 q2;