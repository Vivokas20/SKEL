inputs:
- tests-examples/demo/tables/parts.txt
- tests-examples/demo/tables/supplier.txt
output: tests-examples/demo/tables/demo.out
loc: 1
comment: |
  df1 <- left_join(input2, input1)
  out <- df1 %>% select(PartName) %>% distinct()

#sketch: |
#  T1 = left_join(supplier, [supplier, parts])
#  out = select distinct(PartName)
#sketch: |
#  T1 = left_join(supplier, ??)
#  out = select distinct(PartName)


#sketch: |
#  T1 = ??(supplier, ??)
#  out = select distinct(PartName)
#sketch: |
#  T1 = ??(??, ??*)
#  out = select distinct(PartName)
#sketch: |
#  T1 = ??(??, ??, ??*)
#  out = select distinct(PartName)
#sketch: |
#  T1 = ??(??, ??)
#  out = select distinct(PartName)
#sketch: |
#  T1 = ??(supplier, ??*)
#  out = select distinct(PartName)
#sketch: |
#  T1 = ??(supplier, parts)
#  out = select distinct(PartName)
#sketch: |
#  T1 = ??(supplier, [parts, supplier])
#  out = select distinct(PartName)

#sketch: |
#  T1 = [left_join,semi_join](supplier, [supplier, parts])
#  out = select distinct(PartName)
#sketch: |
#  T1 = [left_join,semi_join](supplier, parts)
#  out = select distinct(PartName)
#sketch: |
#  T1 = [left_join,semi_join](supplier, ??)
#  out = select distinct(PartName)
#sketch: |
#  T1 = [left_join,semi_join](supplier, ??*)
#  out = select distinct(PartName)

#sketch: |
#  T1 = ??
#  out = select distinct(PartName)

full_sketch: |
  T1 = left_join(supplier, parts)
  out = select distinct(PartName)