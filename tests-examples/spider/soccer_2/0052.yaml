db: tests-examples/spider/soccer_2/tables/db.sqlite
inputs:
- tests-examples/spider/soccer_2/tables/college.csv
output: tests-examples/spider/soccer_2/tables/0052.csv
functions:
- count
- avg
columns:
- state
- enr
comment: |-
  SELECT count(DISTINCT state)
  FROM college
  WHERE enr >
      (SELECT avg(enr)
       FROM college)

sql: |-
  SELECT count(DISTINCT state)
  FROM college
  WHERE enr >
      (SELECT avg(enr)
       FROM college)