db: tests-examples/spider/ship_mission/tables/db.sqlite
inputs:
- tests-examples/spider/ship_mission/tables/ship.csv
output: tests-examples/spider/ship_mission/tables/0001.csv
functions:
- count
comment: |-
  SELECT count(*)
  FROM ship

sql: |-
  SELECT count(*)
  FROM ship