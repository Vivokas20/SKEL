db: tests-examples/spider/ship_mission/tables/db.sqlite
inputs:
- tests-examples/spider/ship_mission/tables/ship.csv
output: tests-examples/spider/ship_mission/tables/0007.csv
functions:
- count
comment: |-
  SELECT TYPE,
         COUNT(*)
  FROM ship
  GROUP BY TYPE

sql: |-
  SELECT TYPE,
         COUNT(*)
  FROM ship
  GROUP BY TYPE