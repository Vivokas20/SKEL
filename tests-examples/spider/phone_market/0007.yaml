db: tests-examples/spider/phone_market/tables/db.sqlite
inputs:
- tests-examples/spider/phone_market/tables/phone.csv
output: tests-examples/spider/phone_market/tables/0007.csv
functions:
- count
comment: |-
  SELECT Carrier,
         COUNT(*)
  FROM phone
  GROUP BY Carrier

sql: |-
  SELECT Carrier,
         COUNT(*)
  FROM phone
  GROUP BY Carrier