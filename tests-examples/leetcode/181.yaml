inputs:
  - tests-examples/leetcode/tables/181.csv
output: tests-examples/leetcode/tables/181_o.csv
comment:
  SELECT *
  FROM
    Employee AS a,
    Employee AS b
  WHERE
    a.ManagerId = b.Id
    AND a.Salary > b.Salary



sql: |-
  SELECT * FROM Employee AS a, Employee AS b WHERE a.ManagerId = b.Id AND a.Salary > b.Salary