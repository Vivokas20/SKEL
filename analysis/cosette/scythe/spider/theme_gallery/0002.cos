schema df_artist(artist_id:int, name:str, country:str, year_join:int, age:int);

table df_artist(df_artist);

query q1
`select name,
       age,
       country
from df_artist df_artist`;

query q2
`select input.name,input.age,input.country
 from
  input`;

verify q1 q2;