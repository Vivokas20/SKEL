schema df_services(service_id:int, service_name:str);

table df_services(df_services);

query q1
`select service_name
from df_services df_services`;

query q2
`select input.service_name
 from
  input`;

verify q1 q2;