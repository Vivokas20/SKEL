schema df_vehicles(vehicle_id:int, vehicle_details:str);

table df_vehicles(df_vehicles);

query q1
`select vehicle_details
from df_vehicles df_vehicles
where vehicle_id = 1`;

query q2
`select input.vehicle_details
 from
  input
 where input.vehicle_id = 1.0`;

verify q1 q2;