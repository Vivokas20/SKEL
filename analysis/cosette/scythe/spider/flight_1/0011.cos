schema df_employee(eid:int, name:str, salary:int);

table df_employee(df_employee);

query q1
`select name,
       salary
from df_employee df_employee`;

query q2
`select input.name,input.salary
 from
  input`;

verify q1 q2;