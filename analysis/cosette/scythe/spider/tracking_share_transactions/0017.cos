schema df_lots(lot_id:int, investor_id:int, lot_details:str);

table df_lots(df_lots);

query q1
`select lot_details,
       investor_id
from df_lots df_lots`;

query q2
`select input.lot_details,input.investor_id
 from
  input`;

verify q1 q2;