schema df_club(clubid:int, clubname:str, clubdesc:str, clublocation:str);

table df_club(df_club);

query q1
`select clubdesc
from df_club df_club
where clubname = "tennis df_club"`;

query q2
`select input.clubdesc
 from
  input
 where input.clubname = 'tennis club'`;

verify q1 q2;