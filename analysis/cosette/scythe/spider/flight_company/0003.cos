schema df_airport(id:int, city:str, country:str, iata:str, icao:str, name:str);

table df_airport(df_airport);

query q1
`select id,
       country,
       city,
       name
from df_airport df_airport`;

query q2
`select input.id,input.country,input.city,input.name
 from
  input`;

verify q1 q2;