schema df_staff(staff_id:int, gender:int, first_name:str, last_name:str, email_address:str, phone_number:str);

table df_staff(df_staff);

query q1
`select last_name
from df_staff df_staff
where email_address like "%wrau%"`;

query q2
``;

verify q1 q2;