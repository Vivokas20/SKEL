schema df_pilot(pilot_id:int, name:str, age:int);

table df_pilot(df_pilot);

query q1
`select name
from df_pilot df_pilot
where age >= 25`;

query q2
`select name
from df_pilot df_pilot
where (age >= 25.0)`;

verify q1 q2;