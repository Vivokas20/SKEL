schema df_editor(editor_id:int, name:str, age:str);

table df_editor(df_editor);

query q1
`select name
from df_editor df_editor
where age = 24
  or age = 25`;

query q2
`select name
from df_editor df_editor
where (age >= 24.0
       and age <= 25.0)`;

verify q1 q2;